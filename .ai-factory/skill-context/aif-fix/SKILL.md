# Project Rules for /aif-fix

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-04-05 00:00
> Based on: 0 analyzed patches (tech-stack + convention analysis)

## Rules

### Logging Pattern: Direct console, No LOG_LEVEL
**Source**: codebase convention (src/mcp-entry.ts, rules/base.md)
**Rule**: Use direct `console.log()` / `console.error()` for logging in fixes. Do NOT use the `LOG_LEVEL` env var check pattern (`process.env.LOG_LEVEL === "debug"`) — the project has no structured logging infrastructure, no LOG_LEVEL env var is defined anywhere.

### MCP Stdout Constraint
**Source**: codebase convention (src/mcp-entry.ts, RULES.md)
**Rule**: When fixing code inside `src/mcp/`, NEVER add `console.log()`. Use ONLY `console.error()` for all logging. stdout in the MCP server is exclusively reserved for the MCP protocol — any `console.log()` in `src/mcp/` corrupts the protocol and breaks the MCP client.

### Test Template: Vitest with Russian Descriptions
**Source**: codebase convention (src/embeddings/__tests__/, rules/base.md)
**Rule**: Test suggestions must follow the project Vitest pattern:
- Import: `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
- Test descriptions (it/describe strings) MUST be in Russian language.
- Local imports in test files MUST use `.js` extension (e.g., `from '../module.js'`).
- Group tests with `describe`, individual cases with `it`.
