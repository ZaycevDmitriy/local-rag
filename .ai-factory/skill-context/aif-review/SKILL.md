# Project Rules for /aif-review

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-04-05 00:00
> Based on: 0 analyzed patches (tech-stack + convention analysis)

## Rules

### Check: ESM Import Extensions
**Source**: codebase convention (src/cli.ts, src/mcp-entry.ts, rules/base.md)
**Rule**: Flag any local import path without a `.js` extension. Example: `from './module'` or `from './module/index'` are bugs — must be `from './module.js'` or `from './module/index.js'`. This is a TypeScript ESM project and missing extensions cause runtime errors.

### Check: Module Boundary Violations
**Source**: ARCHITECTURE.md, RULES.md
**Rule**: Flag imports that bypass a module's `index.ts` barrel export. If code outside module `storage/` imports directly from `storage/db.js`, `storage/schema.js`, or any other internal file — that is a violation. Each module's public API is only its `index.ts`. Check cross-module imports carefully.

### Check: MCP Stdout and Thin Adapter
**Source**: ARCHITECTURE.md, RULES.md, codebase convention
**Rule**: Flag two patterns in `src/mcp/` code: (1) Any `console.log()` call — stdout is reserved for MCP protocol, use only `console.error()`. (2) Business logic (search queries, chunking, embedding generation, indexing orchestration) inside `src/mcp/` or `src/commands/` — these are thin adapters and must delegate to orchestrators (`indexer/`, `search/`).
